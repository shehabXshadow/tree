import 'package:flutter/material.dart';

class totat_screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("iam still not here",
      style:TextStyle(
        color:Colors.black87, fontWeight: FontWeight.bold, // Set the font weight here
            fontSize: 16.0, // Set the font size here
      ),),
    );

  }
}
